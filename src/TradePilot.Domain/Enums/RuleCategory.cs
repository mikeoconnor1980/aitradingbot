namespace TradePilot.Domain.Enums;

/// <summary>Functional area of a deterministic strategy rule.</summary>
public enum RuleCategory
{
    Entry,
    Trend,
    Momentum,
    Volatility,
    Risk,
    Sizing,
    Exit,
    Other,
}
